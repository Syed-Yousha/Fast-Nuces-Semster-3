TITLE My First Program (Test.asm)
INCLUDE Irvine32.inc
.data
    array1 BYTE 10h, 20h, 30h, 40h
    array2 BYTE 4 DUP (?)
    tmp BYTE ?

.code
main PROC

    mov ecx, 4 
    mov tmp, 0

my_label:

    mov al, [array1 + ecx - 1] 
    mov edx, 0
    mov dl, tmp

    mov [array2 + edx], al
    inc tmp

loop my_label


mov eax, 0
mov ebx, 0
mov ecx, 0
mov edx, 0

mov al, [array2]
mov bl, [array2 + 1]
mov cl, [array2 + 2]
mov dl, [array2 + 3]


call DumpRegs
exit

main ENDP
END main
